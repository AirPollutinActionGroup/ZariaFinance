name: CI Build & Verify

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ main ]

jobs:
  backend-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Start Database Service
        run: |
          docker compose up -d db
          
      - name: Wait for Database to be Healthy
        run: |
          # Wait up to 30 seconds for the database to be healthy
          for i in {1..30}; do
            if [ "$(docker inspect --format='{{json .State.Health.Status}}' zariya-db)" = "\"healthy\"" ]; then
              echo "Database is healthy!"
              break
            fi
            echo "Waiting for database..."
            sleep 1
          done

      - name: Run Backend Tests in Container
        run: |
          docker compose run --entrypoint "./gradlew test --no-daemon" backend

      - name: Build Backend Docker Image
        run: |
          docker compose build backend

  frontend-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Dependencies
        run: |
          cd frontend
          npm ci

      - name: Run Linting
        run: |
          cd frontend
          npm run lint

      - name: Verify Production Build
        run: |
          cd frontend
          npm run build
